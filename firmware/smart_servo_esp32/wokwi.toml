[wokwi]
version = 1
firmware = ".pio/build/esp32dev/firmware.bin"
elf = ".pio/build/esp32dev/firmware.elf"

# Forward ROS TCP traffic on this computer to the server in the simulated ESP32.
[[net.forward]]
from = "localhost:3333"
to = "target:3333"
